<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>The_end_date_must_be_later_than_the_star</fullName>
    <active>true</active>
    <description>The end date must be later than the start date</description>
    <errorConditionFormula>End_Date__c &lt;= Start_Date__c</errorConditionFormula>
    <errorMessage>The End Date must be later than the Start Date!</errorMessage>
</ValidationRule>
