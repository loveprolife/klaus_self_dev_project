trigger NewPromoterDailyReportTrigger on Promoter_Daily_Report__c (before insert,after insert, before update, after update, before delete, after delete) {
	// new NewPromoterDailyReportTriggerHandler().run();
}