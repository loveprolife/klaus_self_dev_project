<?xml version="1.0" encoding="UTF-8"?>
<Flow xmlns="http://soap.sforce.com/2006/04/metadata">
    <apiVersion>59.0</apiVersion>
    <description>... this subflow centralizes the logic used to re-profile a given sObject result record that had failed. It accepts a recordId representing a sObject result record, verifies its failed status -- and then attempts to reprofile the record.</description>
    <environments>Default</environments>
    <label>Cuneiform: Profiling Results: Reprofile Failed Results: Sub Flow</label>
    <processType>AutoLaunchedFlow</processType>
</Flow>
